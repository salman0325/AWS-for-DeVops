
# 🪣 Amazon S3 (Simple Storage Service)

## 📘 What is Amazon S3?

**Amazon S3 (Simple Storage Service)** is a cloud-based object storage service offered by **AWS (Amazon Web Services)**.
It allows you to **store and retrieve any amount of data** at any time from anywhere on the web.

S3 is commonly used for:

* Backups and archiving
* Hosting static websites
* Storing application data (images, videos, logs, etc.)
* Data analytics and machine learning datasets

---

## 💡 Key Concepts

### 🪣 1. S3 Bucket

A **Bucket** is a container in S3 where you store your **objects (files)**.
Each bucket has a **globally unique name** and resides in a specific **AWS region**.

### 📦 2. S3 Object

An **Object** is the actual data/file you upload to an S3 bucket.
Each object consists of:

* **Data** (the file’s content)
* **Key** (unique name within the bucket)
* **Metadata** (information about the file)
* **Version ID** (if versioning is enabled)
* **ACL (Access Control List)** for permissions

---

## 🧱 S3 Object Properties

| Property          | Description                                                                                      |
| ----------------- | ------------------------------------------------------------------------------------------------ |
| **Key**           | Unique identifier for the object within a bucket (like a file path). Example: `images/photo.jpg` |
| **Size**          | File size in bytes                                                                               |
| **Last Modified** | Timestamp when the object was last updated                                                       |
| **ETag**          | Hash used for data integrity verification                                                        |
| **Storage Class** | Defines cost and availability (Standard, Infrequent Access, Glacier, etc.)                       |
| **Version ID**    | Unique ID if versioning is enabled                                                               |

---

## 🧾 S3 Object Metadata

Metadata provides **extra information** about the object.
There are two types:

1. **System Metadata** – Managed by S3 (e.g., `Content-Type`, `Last-Modified`)
2. **User-defined Metadata** – Custom key-value pairs (e.g., `x-amz-meta-owner: JohnDoe`)

Example:

```
Content-Type: image/png
x-amz-meta-author: John Doe
x-amz-meta-project: marketing-site
```

---

## 🪣 How to Create an S3 Bucket (Step-by-Step)

1. **Login to AWS Management Console**
   Go to [https://aws.amazon.com/console/](https://aws.amazon.com/console/)

2. **Navigate to S3**
   Search for **S3** in the AWS console search bar and open it.

3. **Create Bucket**

   * Click **“Create bucket”**
   * Enter a **unique bucket name**
   * Choose an **AWS Region**
   * (Optional) Configure Versioning, Encryption, or Tags
   * Click **“Create bucket”**

---

## 🌍 How to Make Your Bucket or Objects Public

> ⚠️ **Be careful!** Making your bucket public exposes data to everyone on the internet.

### Method 1: Public Access via Permissions

1. Go to your bucket → **Permissions** tab
2. Under **Block public access**, click **Edit**
3. Uncheck **“Block all public access”**
4. Save changes and confirm

### Method 2: Set Object Public

1. Go to **Objects** tab → select your file
2. Click **Actions → Make public**
3. Copy the **Object URL** to access it publicly (e.g. `https://bucket-name.s3.amazonaws.com/image.png`)

---

## 🪪 Using a Policy Generator to Allow Public Access

If you want all objects in a bucket to be public:

1. Go to **Bucket → Permissions → Bucket Policy**

2. Click **Policy Generator**

3. Use these settings:

   * **Type:** S3 Bucket Policy
   * **Effect:** Allow
   * **Principal:** `*`
   * **Actions:** `GetObject`
   * **Amazon Resource Name (ARN):** `arn:aws:s3:::your-bucket-name/*`

4. Click **Generate Policy** → Copy the JSON → Paste it into the bucket policy editor.

Example Policy:

```json
{
  "Version": "2012-10-17",
  "Statement": [
    {
      "Sid": "PublicReadGetObject",
      "Effect": "Allow",
      "Principal": "*",
      "Action": "s3:GetObject",
      "Resource": "arn:aws:s3:::your-bucket-name/*"
    }
  ]
}
```

---

## 📁 Example Folder Structure

```
my-bucket/
│
├── index.html
├── assets/
│   ├── logo.png
│   └── style.css
└── uploads/
    └── data.csv
```

---

## 🧩 Summary

| Concept           | Description                                    |
| ----------------- | ---------------------------------------------- |
| **S3**            | Object storage service in AWS                  |
| **Bucket**        | Container for storing data                     |
| **Object**        | Actual data/file stored in S3                  |
| **Key**           | Unique name identifying the object             |
| **Metadata**      | Additional info about the object               |
| **Bucket Policy** | Defines access permissions for the bucket      |
| **Public Access** | Allows anyone to view/download objects via URL |

